class FileItem {
  String imageFilePath;
  String textFilePath;
  String temporaryDescription;

  FileItem({required this.imageFilePath, required this.textFilePath, required this.temporaryDescription});
}